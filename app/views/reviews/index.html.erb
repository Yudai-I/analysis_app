<div>商品名：<%= @product %></div>

<%= @ans %>

<%= form_with url: reviews_path, local: true, method: :get, class: "row g-3 align-items-center" do |f| %>
  <div class="col-auto">
    <%= f.text_field :url, class: "form-control", placeholder: "URLを入力してください" %>
  </div>
  <div class="col-auto">
    <%= f.submit "分析する", class: "btn btn-outline-dark" %>
  </div>
<% end %>

